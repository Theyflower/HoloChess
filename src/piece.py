def empty():
	'''returns a piece to represent an empty tile, probably just none'''
	#@todo(aaron) make this function return a piece representing the empty tilestate
	#				probably returned by the 'default' constructor
	return None